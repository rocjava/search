server:
  port: 21020

spring:
  application:
    name: oms-search
  flyway:
    baseline-on-migrate: true
    check-location: false
  data:
    elasticsearch:
      cluster-name: my-application
      cluster-nodes: 127.0.0.1:9300
      repositories:
      enabled=true: true